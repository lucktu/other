Router B ping supernode & Router A's edgeip, Going from normal to disconnected to normal.

----------------------------------------------------

Router A (edge A runs inside ) ping supernode is normal, continuous,
Router A ping supernode
Router B (edge B runs inside ) ping supernode is normal, continuous too,
Router B ping supernode & Router A's edgeip

Router A's network environment is very poor. Others can¡®t P2P with it. 
